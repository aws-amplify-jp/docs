import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'UI を構築する',
  description: 'UI を構築する',
  platforms: [
    'android',
    'angular',
    'flutter',
    'javascript',
    'nextjs',
    'react',
    'react-native',
    'swift',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

<Callout>
注: このページは Gen 1 ドキュメントの最新バージョン専用です。最新の情報と機能にアクセスできるようにするため、選択したフレームワークまたは言語の[最新バージョンを表示するにはこちら](/gen1/[platform]/build-ui)にアクセスすることをお勧めします。
</Callout>