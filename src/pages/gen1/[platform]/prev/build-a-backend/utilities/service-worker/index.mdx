import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Service Worker',
  description: 'AWS Amplify ServiceWorker クラスは、ブラウザでService Workerを登録し、postMessage イベントを通じて通信できるようにしており、Push API とアナリティクスを使用して充実したオフライン体験を構築できます。',
  platforms: [
    'javascript',
    'angular',
    'nextjs',
    'react',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib-v1/utilities/serviceworker.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>