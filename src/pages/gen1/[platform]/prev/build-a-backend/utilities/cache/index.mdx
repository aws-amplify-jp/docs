import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'キャッシュ',
  description: 'Amplify Cache モジュールは、JavaScript開発者がデータを優先度と有効期限の設定で保存できる汎用LRUキャッシュを提供します。',
  platforms: [
    'javascript',
    'react-native',
    'angular',
    'nextjs',
    'react',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib-v1/utilities/cache.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>

import reactnative0 from '/src/fragments/lib-v1/utilities/cache.mdx';

<Fragments fragments={{ 'react-native': reactnative0 }} />