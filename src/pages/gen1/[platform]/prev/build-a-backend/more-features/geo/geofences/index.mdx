import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'ジオフェンスを使用する',
  description: 'Amplify Geoを使用してアプリケーションでジオフェンスをプロビジョニングおよび管理します。',
  platforms: [
    'javascript',
    'angular',
    'nextjs',
    'react',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib-v1/geo/js/geofences.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>