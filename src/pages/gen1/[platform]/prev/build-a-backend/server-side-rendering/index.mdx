import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'サーバーサイドレンダリング',
  description: 'Next.js サーバーサイドランタイムから Amplify カテゴリ API を使用します。',
  platforms: [
    'angular',
    'javascript',
    'nextjs',
    'react',
    'vue'
  ],
  route: '/gen1/[platform]/prev/build-a-backend/server-side-rendering'
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib-v1/ssr/js/getting-started.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>