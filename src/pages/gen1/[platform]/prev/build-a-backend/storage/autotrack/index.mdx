import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'イベントを自動的に追跡する',
  description: 'アップロードやダウンロードなどのストレージイベントの自動追跡を有効にできます。これを有効にすると、ストレージイベントが自動的にAmazon Pinpointに送信され、AWS Pinpointコンソールのカスタムイベント内で表示できるようになります。',
  platforms: [
    'javascript',
    'angular',
    'nextjs',
    'react',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib-v1/storage/js/autotrack.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>