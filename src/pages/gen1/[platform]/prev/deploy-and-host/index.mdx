import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'デプロイとホスト',
  description: 'デプロイとホスト',
  platforms: [
    'android',
    'angular',
    'flutter',
    'javascript',
    'nextjs',
    'react',
    'react-native',
    'swift',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

<Callout>
注記: このページは、Gen 1 ドキュメントの最新バージョンのみです。最新の情報と機能にアクセスできるように、選択したフレームワークまたは言語の[最新バージョンはこちら](/gen1/[platform]/deploy-and-host)を参照することをお勧めします。
</Callout>