import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'マップを使用する',
  description: 'マップディスプレイ、API、その他の機能の使用。',
  platforms: [
    'javascript',
    'swift',
    'android',
    'angular',
    'nextjs',
    'react',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import nativeCommonMaps from '/src/fragments/lib/geo/native_common/maps/common.mdx';
import jsMaps from '/src/fragments/lib/geo/js/maps.mdx';

<Fragments
  fragments={{
    android: nativeCommonMaps,
    swift: nativeCommonMaps,
    javascript: jsMaps,
    angular: jsMaps,
    nextjs: jsMaps,
    react: jsMaps,
    vue: jsMaps
  }}
/>