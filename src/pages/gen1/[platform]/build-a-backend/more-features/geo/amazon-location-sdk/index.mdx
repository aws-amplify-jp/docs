import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Amazon Location Service SDK を使用する',
  description: 'Amplify が機能を提供していない特殊なユースケースでは、エスケープハッチを使用して Amazon Location Service の低レベルクライアントインスタンスにアクセスできます。',
  platforms: [
    'javascript',
    'swift',
    'android',
    'angular',
    'nextjs',
    'react',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import androidEscapeHatch from '/src/fragments/lib/geo/android/escapehatch.mdx';
import iosEscapeHatch from '/src/fragments/lib/geo/ios/escapehatch.mdx';
import jsEscapeHatch from '/src/fragments/lib/geo/js/escapehatch.mdx';

<Fragments
  fragments={{
    android: androidEscapeHatch,
    swift: iosEscapeHatch,
    javascript: jsEscapeHatch,
    angular: jsEscapeHatch,
    nextjs: jsEscapeHatch,
    react: jsEscapeHatch,
    vue: jsEscapeHatch
  }}
/>