import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'ローカルストレージを変更する',
  description: 'デバイス上のログローカルストレージを変更する',
  platforms: [
    'swift',
    'android'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import commonRoute from '/src/fragments/lib/logging/common/change_local_storage/change_local_storage.mdx';

<Fragments
  fragments={{
    android: commonRoute,
    swift: commonRoute
  }}
/>