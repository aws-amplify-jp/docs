import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Amplify プッシュ通知のセットアップ',
  description: 'Amplify プッシュ通知の使用',
  platforms: ['react-native', 'android', 'flutter', 'swift']
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import commonRoute from '/src/fragments/lib/push-notifications/common/getting_started/getting-started.mdx';

<Fragments
  fragments={{
    android: commonRoute,
    flutter: commonRoute,
    swift: commonRoute,
    'react-native': commonRoute
  }}
/>