(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[91838],{7072:(e,t,n)=>{"use strict";n.d(t,{c:()=>p});var r=n(66575),a=n(53276),i=n(66168),s=n(14232),o=n(40574),l=n(82653);let p=(0,o.m)(({className:e,orientation:t="horizontal",size:n,label:o,...p},d)=>{let u=(0,r.x)(a.P.Divider,(0,i.BY)(a.P.Divider,t),(0,i.BY)(a.P.Divider,n),e);return s.createElement(l.S,{"aria-orientation":t,as:"hr",className:u,"data-label":o,ref:d,...p})});p.displayName="Divider"},27808:(e,t,n)=>{"use strict";n.d(t,{t:()=>g});var r=n(14232),a=n.t(r,2),i=n(40574),s=n(66168),o=n(66575),l=n(53276),p=n(82653);let d=r.createContext({groupId:"",activeTab:"",setActiveTab:()=>{}}),u=(0,i.m)(({className:e,value:t,children:n,onClick:a,as:i="button",role:u="tab",...c},y)=>{let{activeTab:h,setActiveTab:m,groupId:P}=r.useContext(d),E=t;"string"==typeof E&&(E=E.replace(" ","-"));let g=h===t;return r.createElement(p.S,{...c,role:u,as:i,id:`${P}-tab-${E}`,"aria-selected":g,"aria-controls":`${P}-panel-${E}`,tabIndex:g?void 0:-1,className:(0,o.x)(l.P.TabsItem,(0,s.Wt)(l.P.TabsItem,"active",h===t),e),ref:y,onClick:e=>{(0,s.Tn)(a)&&a?.(e),m(t)}},n)});u.displayName="Tabs.Item";let c=(0,i.m)(({className:e,children:t,indicatorPosition:n,spacing:a,role:i="tablist",...u},c)=>{let y=r.useRef(null),{activeTab:h,setActiveTab:m}=r.useContext(d);r.useImperativeHandle(c,()=>y.current);let P=r.useMemo(()=>r.Children.toArray(t).map(e=>{if(e&&r.isValidElement(e))return e.props.value}).filter(e=>!!e),[t]),E=P.indexOf(h),g=r.useCallback(()=>{let e=E===P.length-1?0:E+1,t=y.current?.querySelectorAll("button")??[];for(;t[e].disabled;)e===P.length-1?e=0:e++;let n=P[e];if(n){m(n);let r=t[e];r?.focus(),r?.click()}},[E,m,P]),f=r.useCallback(()=>{let e=0===E?P.length-1:E-1,t=y.current?.querySelectorAll("button")??[];for(;t[e].disabled;)0===e?e=P.length-1:e--;let n=P[e];if(n){m(n);let r=t[e];r?.focus(),r?.click()}},[E,m,P]),w=r.useCallback(e=>{switch(e.key){case"ArrowLeft":e.preventDefault(),e.stopPropagation(),f();break;case"ArrowUp":case"ArrowRight":e.preventDefault(),e.stopPropagation(),g()}},[f,g]);return r.createElement(p.S,{...u,role:i,onKeyDown:w,className:(0,o.x)(l.P.TabsList,n?(0,s.BY)(l.P.TabsList,n):null,a?(0,s.BY)(l.P.TabsList,a):null,e),ref:y},t)});c.displayName="Tabs.List";let y=(0,i.m)(({className:e,value:t,children:n,role:a="tabpanel",...i},u)=>{let{activeTab:c,isLazy:y,groupId:h}=r.useContext(d);if(y&&c!==t)return null;let m=t;return"string"==typeof m&&(m=m.replace(" ","-")),r.createElement(p.S,{...i,role:a,id:`${h}-panel-${m}`,"aria-labelledby":`${h}-tab-${m}`,className:(0,o.x)(l.P.TabsPanel,(0,s.Wt)(l.P.TabsPanel,"active",c===t),e),ref:u},n)});y.displayName="Tabs.Panel";let h=globalThis?.document?r.useLayoutEffect:()=>{},m=a["useId".toString()]||(()=>void 0),P=0,E=(0,i.m)(({children:e,defaultValue:t,className:n,value:a,onValueChange:i,isLazy:u,...c},y)=>{let E=(e=>{let[t,n]=r.useState(m());return h(()=>{n(e=>e??String(P++))},[void 0]),(void 0)??(t?`amplify-id-${t}`:"")})(),g=void 0!==a,[f,w]=r.useState(()=>g?a:t),v=g?a:f??"",x=r.useCallback(e=>{(0,s.Tn)(i)&&i(e),g||w(e)},[i,g]),R=r.useMemo(()=>({activeTab:v,isLazy:u,setActiveTab:x,groupId:E}),[v,x,u,E]);return r.createElement(d.Provider,{value:R},r.createElement(p.S,{...c,ref:y,className:(0,o.x)(n,l.P.Tabs)},e))});E.displayName="Tabs.Container";let g=Object.assign((0,i.m)(({items:e,indicatorPosition:t,justifyContent:n,spacing:a,...i},s)=>r.createElement(E,{...i,ref:s},r.createElement(c,{indicatorPosition:t,justifyContent:n,spacing:a},e?.map(({value:e,label:t,content:n,...a})=>r.createElement(u,{...a,key:e,value:e},t))),e?.map(({value:e,content:t,isDisabled:n})=>r.createElement(y,{key:e,value:e,isDisabled:n},t)))),{Item:u,List:c,Panel:y,Container:E});g.displayName="Tabs"},83089:(e,t,n)=>{"use strict";n.r(t),n.d(t,{__N_SSG:()=>u,default:()=>h,meta:()=>c});var r=n(37876),a=n(9434);function i(e){let t=Object.assign({p:"p",a:"a",pre:"pre",code:"code",h2:"h2",em:"em"},(0,a.R)(),e.components),{Callout:n,BlockSwitcher:i,Block:s}=t;return s||o("Block",!0),i||o("BlockSwitcher",!0),n||o("Callout",!0),(0,r.jsxs)(r.Fragment,{children:[(0,r.jsxs)(t.p,{children:["The GraphQL API category can be used with ",(0,r.jsx)(t.a,{href:"https://tanstack.com/query/latest/docs/react/overview",children:"TanStack Query"})," to implement optimistic UI, allowing CRUD operations to be rendered immediately on the UI before the request roundtrip has completed. Using the GraphQL API with TanStack additionally makes it easy to render loading and error states, and allows you to rollback changes on the UI when API calls are unsuccessful."]}),"\n",(0,r.jsx)(t.p,{children:"In the following examples we'll create a list view that optimistically renders newly created items, and a detail view that optimistically renders updates and deletes."}),"\n",(0,r.jsx)(n,{children:(0,r.jsxs)(t.p,{children:["For more details on TanStack Query, including requirements, supported browsers, and advanced usage, see the ",(0,r.jsx)(t.a,{href:"https://tanstack.com/query/latest/docs/react/overview",children:"TanStack Query documentation"}),".\nFor complete guidance on how to implement optimistic updates with TanStack Query, see the ",(0,r.jsx)(t.a,{href:"https://tanstack.com/query/latest/docs/react/guides/optimistic-updates",children:"TanStack Query Optimistic UI Documentation"}),".\nFor more on the Amplify GraphQL API, see the ",(0,r.jsx)(t.a,{href:"/gen1/%5Bplatform%5D/build-a-backend/graphqlapi/set-up-graphql-api/",children:"API documentation"}),"."]})}),"\n",(0,r.jsx)(t.p,{children:"To get started, run the following command in an existing Amplify project with a React frontend:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-bash",children:"# Install TanStack Query\nnpm i @tanstack/react-query\n\n# Select default configuration\namplify add api              \n"})}),"\n",(0,r.jsx)(t.p,{children:"When prompted, use the following schema:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-graphql",children:"type RealEstateProperty @model @auth(rules: [{ allow: public }]) {\n  id: ID!\n  name: String!\n  address: String\n}\n"})}),"\n",(0,r.jsxs)(t.p,{children:["The schema file can also be found under ",(0,r.jsx)(t.code,{children:"amplify/backend/api/[name of project]/schema.graphql"}),".\nSave the schema and run ",(0,r.jsx)(t.code,{children:"amplify push"})," to deploy the changes. For the purposes of this guide, we'll build a Real Estate Property listing application."]}),"\n",(0,r.jsx)(t.p,{children:"Next, at the root of your project, add the required TanStack Query imports, and create a client:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:'import { QueryClient, QueryClientProvider } from "@tanstack/react-query";\nimport { ReactQueryDevtools } from "@tanstack/react-query-devtools";\n\n// Create a client\nconst queryClient = new QueryClient()\n'})}),"\n",(0,r.jsx)(t.p,{children:"Next, wrap your app in the client provider:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:"<QueryClientProvider client={queryClient}>\n  <App />\n  <ReactQueryDevtools initialIsOpen={false} />\n</QueryClientProvider>\n"})}),"\n",(0,r.jsx)(n,{children:(0,r.jsxs)(t.p,{children:["TanStack Query Devtools are not required, but are a useful resource for debugging and understanding how TanStack works under the hood. By default, React Query Devtools are only included in bundles when ",(0,r.jsx)(t.code,{children:"process.env.NODE_ENV === 'development'"}),", meaning that no additional configuration is required to exclude them from a production build.\nFor more information on the TanStack Query Devtools, visit the ",(0,r.jsx)(t.a,{href:"https://tanstack.com/query/latest/docs/react/devtools",children:"TanStack Query Devtools docs"})]})}),"\n",(0,r.jsx)(n,{children:(0,r.jsxs)(t.p,{children:["For the complete working example, including required imports and React component state management, see the ",(0,r.jsx)(t.a,{href:"#complete-example",children:"Complete Example"})," below."]})}),"\n",(0,r.jsx)(t.h2,{id:"how-to-use-tanstack-query-query-keys-with-the-graphql-api",children:"How to use TanStack Query query keys with the GraphQL API"}),"\n",(0,r.jsx)(t.p,{children:"TanStack Query manages query caching based on the query keys you specify. A query key must be an array. The array can contain a single string or multiple strings and nested objects. The query key must be serializable, and unique to the query's data."}),"\n",(0,r.jsxs)(t.p,{children:["When using TanStack to render optimistic UI with the GraphQL API, it is important to note that different query keys must be used depending on the API operation. When retrieving a list of items, a single string is used (e.g. ",(0,r.jsx)(t.code,{children:'queryKey: ["realEstateProperties"]'}),"). This query key is also used to optimistically render a newly created item. When updating or deleting an item, the query key must also include the unique identifier for the record being deleted or updated (e.g. ",(0,r.jsx)(t.code,{children:'queryKey: ["realEstateProperties", newRealEstateProperty.id]'}),")."]}),"\n",(0,r.jsxs)(t.p,{children:["For more detailed information on query keys, see the ",(0,r.jsx)(t.a,{href:"https://tanstack.com/query/v4/docs/react/guides/query-keys",children:"TanStack Query documentation"}),"."]}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-rendering-a-list-of-records",children:"Optimistically rendering a list of records"}),"\n",(0,r.jsxs)(t.p,{children:["To optimistically render a list of items returned from the GraphQL API, use the TanStack ",(0,r.jsx)(t.code,{children:"useQuery"})," hook, passing in the GraphQL API query as the ",(0,r.jsx)(t.code,{children:"queryFn"})," parameter. The following example creates a query to retrieve all records from the API. We'll use ",(0,r.jsx)(t.code,{children:"realEstateProperties"})," as the query key, which will be the same key we use to optimistically render a newly created item."]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:"import { generateClient } from 'aws-amplify/api'\n\nconst client = generateClient()\n\nconst {\n  data: realEstateProperties,\n  isLoading,\n  isSuccess,\n  isError: isErrorQuery,\n} = useQuery({\n  queryKey: [\"realEstateProperties\"],\n  queryFn: async () => {\n    const response = await client.graphql({\n      query: queries.listRealEstateProperties,\n    });\n\n    const allRealEstateProperties =\n      response?.data?.listRealEstateProperties?.items;\n\n    if (!allRealEstateProperties) return null;\n\n    return allRealEstateProperties;\n  },\n});\n"})}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-rendering-a-newly-created-record",children:"Optimistically rendering a newly created record"}),"\n",(0,r.jsxs)(t.p,{children:["To optimistically render a newly created record returned from the GraphQL API, use the TanStack ",(0,r.jsx)(t.code,{children:"useMutation"})," hook, passing in the GraphQL API mutation as the ",(0,r.jsx)(t.code,{children:"mutationFn"})," parameter. We'll use the same query key used by the ",(0,r.jsx)(t.code,{children:"useQuery"})," hook (",(0,r.jsx)(t.code,{children:"realEstateProperties"}),") as the query key to optimistically render a newly created item.\nWe'll use the ",(0,r.jsx)(t.code,{children:"onMutate"})," function to update the cache directly, as well as the ",(0,r.jsx)(t.code,{children:"onError"})," function to rollback changes when a request fails."]}),"\n",(0,r.jsxs)(i,{children:[(0,r.jsx)(s,{name:"TypeScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:'import { generateClient } from \'aws-amplify/api\'\n\nconst client = generateClient()\n\nconst createMutation = useMutation({\n  mutationFn: async (realEstatePropertyDetails: CreateRealEstatePropertyInput) => {\n    const response = await client.graphql({\n      query: mutations.createRealEstateProperty,\n      variables: { input: realEstatePropertyDetails },\n    });\n\n    const newRealEstateProperty = response?.data?.createRealEstateProperty;\n    return newRealEstateProperty;\n  },\n  // When mutate is called:\n  onMutate: async (newRealEstateProperty) => {\n    // Cancel any outgoing refetches\n    // (so they don\'t overwrite our optimistic update)\n    await queryClient.cancelQueries({ queryKey: ["realEstateProperties"] });\n\n    // Snapshot the previous value\n    const previousRealEstateProperties = queryClient.getQueryData([\n      "realEstateProperties",\n    ]);\n\n    // Optimistically update to the new value\n    if (previousRealEstateProperties) {\n      queryClient.setQueryData(["realEstateProperties"], (old: RealEstateProperty[]) => [\n        ...old,\n        newRealEstateProperty,\n      ]);\n    }\n\n    // Return a context object with the snapshotted value\n    return { previousRealEstateProperties };\n  },\n  // If the mutation fails,\n  // use the context returned from onMutate to rollback\n  onError: (err, newRealEstateProperty, context) => {\n    console.error("Error saving record:", err, newRealEstateProperty);\n    if (context?.previousRealEstateProperties) {\n      queryClient.setQueryData(\n        ["realEstateProperties"],\n        context.previousRealEstateProperties\n      );\n    }\n  },\n  // Always refetch after error or success:\n  onSettled: () => {\n    queryClient.invalidateQueries({ queryKey: ["realEstateProperties"] });\n  },\n});\n'})})}),(0,r.jsx)(s,{name:"JavaScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:'import { generateClient } from \'aws-amplify/api\'\n\nconst client = generateClient()\n\nconst createMutation = useMutation({\n  mutationFn: async (realEstatePropertyDetails) => {\n    const response = await client.graphql({\n      query: mutations.createRealEstateProperty,\n      variables: { input: realEstatePropertyDetails },\n    });\n\n    const newRealEstateProperty = response?.data?.createRealEstateProperty;\n    return newRealEstateProperty;\n  },\n  // When mutate is called:\n  onMutate: async (newRealEstateProperty) => {\n    // Cancel any outgoing refetches\n    // (so they don\'t overwrite our optimistic update)\n    await queryClient.cancelQueries({ queryKey: ["realEstateProperties"] });\n\n    // Snapshot the previous value\n    const previousRealEstateProperties = queryClient.getQueryData([\n      "realEstateProperties",\n    ]);\n\n    // Optimistically update to the new value\n    if (previousRealEstateProperties) {\n      queryClient.setQueryData(["realEstateProperties"], (old) => [\n        ...old,\n        newRealEstateProperty,\n      ]);\n    }\n\n    // Return a context object with the snapshotted value\n    return { previousRealEstateProperties };\n  },\n  // If the mutation fails,\n  // use the context returned from onMutate to rollback\n  onError: (err, newRealEstateProperty, context) => {\n    console.error("Error saving record:", err, newRealEstateProperty);\n    if (context?.previousRealEstateProperties) {\n      queryClient.setQueryData(\n        ["realEstateProperties"],\n        context.previousRealEstateProperties\n      );\n    }\n  },\n  // Always refetch after error or success:\n  onSettled: () => {\n    queryClient.invalidateQueries({ queryKey: ["realEstateProperties"] });\n  },\n});\n'})})})]}),"\n",(0,r.jsx)(t.h2,{id:"querying-a-single-item-with-tanstack-query",children:"Querying a single item with TanStack Query"}),"\n",(0,r.jsxs)(t.p,{children:["To optimistically render updates on a single item, we'll first retrieve the item from the API. We'll use the ",(0,r.jsx)(t.code,{children:"useQuery"})," hook, passing in the GraphQL API query as the ",(0,r.jsx)(t.code,{children:"queryFn"})," parameter. For the query key, we'll use a combination of ",(0,r.jsx)(t.code,{children:"realEstateProperties"})," and the record's unique identifier."]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:"import { generateClient } from 'aws-amplify/api'\n\nconst client = generateClient()\n\nconst {\n  data: realEstateProperty,\n  isLoading,\n  isSuccess,\n  isError: isErrorQuery,\n} = useQuery({\n  queryKey: [\"realEstateProperties\", currentRealEstatePropertyId],\n  queryFn: async () => {\n    if (!currentRealEstatePropertyId) { return }\n\n    const response = await client.graphql({\n      query: queries.getRealEstateProperty,\n      variables: { id: currentRealEstatePropertyId },\n    });\n\n    return response.data?.getRealEstateProperty;\n  },\n});\n"})}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-render-updates-for-a-record",children:"Optimistically render updates for a record"}),"\n",(0,r.jsxs)(t.p,{children:["To optimistically render GraphQL API updates for a single record, use the TanStack ",(0,r.jsx)(t.code,{children:"useMutation"})," hook, passing in the GraphQL API update mutation as the ",(0,r.jsx)(t.code,{children:"mutationFn"})," parameter. We'll use the same query key combination used by the single record ",(0,r.jsx)(t.code,{children:"useQuery"})," hook (",(0,r.jsx)(t.code,{children:"realEstateProperties"})," and the record's ",(0,r.jsx)(t.code,{children:"id"}),") as the query key to optimistically render the updates.\nWe'll use the ",(0,r.jsx)(t.code,{children:"onMutate"})," function to update the cache directly, as well as the ",(0,r.jsx)(t.code,{children:"onError"})," function to rollback changes when a request fails."]}),"\n",(0,r.jsx)(n,{children:(0,r.jsxs)(t.p,{children:["When directly interacting with the cache via the ",(0,r.jsx)(t.code,{children:"onMutate"})," function, it should be noted that the ",(0,r.jsx)(t.code,{children:"newRealEstateProperty"})," parameter only includes the fields that are being updated, until the final return from the GraphQL API returns all fields for the record. When calling ",(0,r.jsx)(t.code,{children:"setQueryData"}),", include the previous values for all fields in addition to the newly updated fields to avoid only rendering optimistic values for updated fields on the UI."]})}),"\n",(0,r.jsxs)(i,{children:[(0,r.jsx)(s,{name:"TypeScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:'import { generateClient } from \'aws-amplify/api\'\n\nconst client = generateClient()\n\nconst updateMutation = useMutation({\n  mutationFn: async (\n    realEstatePropertyDetails: UpdateRealEstatePropertyInput\n  ) => {\n    const response = await client.graphql({\n      query: mutations.updateRealEstateProperty,\n      variables: { input: realEstatePropertyDetails },\n    });\n\n    return response?.data?.updateRealEstateProperty;\n  },\n  // When mutate is called:\n  onMutate: async (newRealEstateProperty) => {\n    // Cancel any outgoing refetches\n    // (so they don\'t overwrite our optimistic update)\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties", newRealEstateProperty.id],\n    });\n\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties"],\n    });\n\n    // Snapshot the previous value\n    const previousRealEstateProperty = queryClient.getQueryData([\n      "realEstateProperties",\n      newRealEstateProperty.id,\n    ]);\n\n    // Optimistically update to the new value\n    if (previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", newRealEstateProperty.id],\n        /**\n         * `newRealEstateProperty` will at first only include updated values for\n         * the record. To avoid only rendering optimistic values for updated\n         * fields on the UI, include the previous values for all fields:\n         */\n        { ...previousRealEstateProperty, ...newRealEstateProperty }\n      );\n    }\n\n    // Return a context with the previous and new realEstateProperty\n    return { previousRealEstateProperty, newRealEstateProperty };\n  },\n  // If the mutation fails, use the context we returned above\n  onError: (err, newRealEstateProperty, context) => {\n    console.error("Error updating record:", err, newRealEstateProperty);\n    if (context?.previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", context.newRealEstateProperty.id],\n        context.previousRealEstateProperty\n      );\n    }\n  },\n  // Always refetch after error or success:\n  onSettled: (newRealEstateProperty) => {\n    if (newRealEstateProperty) {\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties", newRealEstateProperty.id],\n      });\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties"],\n      });\n    }\n  },\n});\n'})})}),(0,r.jsx)(s,{name:"JavaScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:'import { generateClient } from \'aws-amplify/api\'\n\nconst client = generateClient()\n\nconst updateMutation = useMutation({\n  mutationFn: async (realEstatePropertyDetails) => {\n    const response = await client.graphql({\n      query: mutations.updateRealEstateProperty,\n      variables: { input: realEstatePropertyDetails },\n    });\n\n    return response?.data?.updateRealEstateProperty;\n  },\n  // When mutate is called:\n  onMutate: async (newRealEstateProperty) => {\n    // Cancel any outgoing refetches\n    // (so they don\'t overwrite our optimistic update)\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties", newRealEstateProperty.id],\n    });\n\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties"],\n    });\n\n    // Snapshot the previous value\n    const previousRealEstateProperty = queryClient.getQueryData([\n      "realEstateProperties",\n      newRealEstateProperty.id,\n    ]);\n\n    // Optimistically update to the new value\n    if (previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", newRealEstateProperty.id],\n        /**\n         * `newRealEstateProperty` will at first only include updated values for\n         * the record. To avoid only rendering optimistic values for updated\n         * fields on the UI, include the previous values for all fields:\n         */\n        { ...previousRealEstateProperty, ...newRealEstateProperty }\n      );\n    }\n\n    // Return a context with the previous and new realEstateProperty\n    return { previousRealEstateProperty, newRealEstateProperty };\n  },\n  // If the mutation fails, use the context we returned above\n  onError: (err, newRealEstateProperty, context) => {\n    console.error("Error updating record:", err, newRealEstateProperty);\n    if (context?.previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", context.newRealEstateProperty.id],\n        context.previousRealEstateProperty\n      );\n    }\n  },\n  // Always refetch after error or success:\n  onSettled: (newRealEstateProperty) => {\n    if (newRealEstateProperty) {\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties", newRealEstateProperty.id],\n      });\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties"],\n      });\n    }\n  },\n});\n'})})})]}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-render-deleting-a-record",children:"Optimistically render deleting a record"}),"\n",(0,r.jsxs)(t.p,{children:["To optimistically render a GraphQL API delete of a single record, use the TanStack ",(0,r.jsx)(t.code,{children:"useMutation"})," hook, passing in the GraphQL API delete mutation as the ",(0,r.jsx)(t.code,{children:"mutationFn"})," parameter. We'll use the same query key combination used by the single record ",(0,r.jsx)(t.code,{children:"useQuery"})," hook (",(0,r.jsx)(t.code,{children:"realEstateProperties"})," and the record's ",(0,r.jsx)(t.code,{children:"id"}),") as the query key to optimistically render the updates.\nWe'll use the ",(0,r.jsx)(t.code,{children:"onMutate"})," function to update the cache directly, as well as the ",(0,r.jsx)(t.code,{children:"onError"})," function to rollback changes when a delete fails."]}),"\n",(0,r.jsxs)(i,{children:[(0,r.jsx)(s,{name:"TypeScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:'import { generateClient } from \'aws-amplify/api\'\n\nconst client = generateClient()\n\nconst deleteMutation = useMutation({\n  mutationFn: async (\n    realEstatePropertyDetails: DeleteRealEstatePropertyInput\n  ) => {\n    const response = await client.graphql({\n      query: mutations.deleteRealEstateProperty,\n      variables: { input: realEstatePropertyDetails },\n    });\n\n    return response?.data?.deleteRealEstateProperty;\n  },\n  // When mutate is called:\n  onMutate: async (newRealEstateProperty) => {\n    // Cancel any outgoing refetches\n    // (so they don\'t overwrite our optimistic update)\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties", newRealEstateProperty.id],\n    });\n\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties"],\n    });\n\n    // Snapshot the previous value\n    const previousRealEstateProperty = queryClient.getQueryData([\n      "realEstateProperties",\n      newRealEstateProperty.id,\n    ]);\n\n    // Optimistically update to the new value\n    if (previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", newRealEstateProperty.id],\n        newRealEstateProperty\n      );\n    }\n\n    // Return a context with the previous and new realEstateProperty\n    return { previousRealEstateProperty, newRealEstateProperty };\n  },\n  // If the mutation fails, use the context we returned above\n  onError: (err, newRealEstateProperty, context) => {\n    console.error("Error deleting record:", err, newRealEstateProperty);\n    if (context?.previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", context.newRealEstateProperty.id],\n        context.previousRealEstateProperty\n      );\n    }\n  },\n  // Always refetch after error or success:\n  onSettled: (newRealEstateProperty) => {\n    if (newRealEstateProperty) {\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties", newRealEstateProperty.id],\n      });\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties"],\n      });\n    }\n  },\n});\n'})})}),(0,r.jsx)(s,{name:"JavaScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:'import { generateClient } from \'aws-amplify/api\'\n\nconst client = generateClient()\n\nconst deleteMutation = useMutation({\n  mutationFn: async (realEstatePropertyDetails) => {\n    const response = await client.graphql({\n      query: mutations.deleteRealEstateProperty,\n      variables: { input: realEstatePropertyDetails },\n    });\n\n    return response?.data?.deleteRealEstateProperty;\n  },\n  // When mutate is called:\n  onMutate: async (newRealEstateProperty) => {\n    // Cancel any outgoing refetches\n    // (so they don\'t overwrite our optimistic update)\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties", newRealEstateProperty.id],\n    });\n\n    await queryClient.cancelQueries({\n      queryKey: ["realEstateProperties"],\n    });\n\n    // Snapshot the previous value\n    const previousRealEstateProperty = queryClient.getQueryData([\n      "realEstateProperties",\n      newRealEstateProperty.id,\n    ]);\n\n    // Optimistically update to the new value\n    if (previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", newRealEstateProperty.id],\n        newRealEstateProperty\n      );\n    }\n\n    // Return a context with the previous and new realEstateProperty\n    return { previousRealEstateProperty, newRealEstateProperty };\n  },\n  // If the mutation fails, use the context we returned above\n  onError: (err, newRealEstateProperty, context) => {\n    console.error("Error deleting record:", err, newRealEstateProperty);\n    if (context?.previousRealEstateProperty) {\n      queryClient.setQueryData(\n        ["realEstateProperties", context.newRealEstateProperty.id],\n        context.previousRealEstateProperty\n      );\n    }\n  },\n  // Always refetch after error or success:\n  onSettled: (newRealEstateProperty) => {\n    if (newRealEstateProperty) {\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties", newRealEstateProperty.id],\n      });\n      queryClient.invalidateQueries({\n        queryKey: ["realEstateProperties"],\n      });\n    }\n  },\n});\n'})})})]}),"\n",(0,r.jsx)(t.h2,{id:"loading-and-error-states-for-optimistically-rendered-data",children:"Loading and error states for optimistically rendered data"}),"\n",(0,r.jsxs)(t.p,{children:["Both ",(0,r.jsx)(t.code,{children:"useQuery"})," and ",(0,r.jsx)(t.code,{children:"useMutation"})," return ",(0,r.jsx)(t.code,{children:"isLoading"})," and ",(0,r.jsx)(t.code,{children:"isError"})," states that indicate the current state of the query or mutation. You can use these states to render loading and error indicators."]}),"\n",(0,r.jsxs)(t.p,{children:["In addition to operation-specific loading states, TanStack Query provides a ",(0,r.jsxs)(t.a,{href:"https://www.tanstack.com/query/v4/docs/react/guides/background-fetching-indicators#displaying-global-background-fetching-loading-state",children:[(0,r.jsx)(t.code,{children:"useIsFetching"})," hook"]}),". For the purposes of this demo, we show a global loading indicator in the ",(0,r.jsx)(t.a,{href:"#complete-example",children:"Complete Example"})," when ",(0,r.jsx)(t.em,{children:"any"})," queries are fetching (including in the background) in order to help visualize what TanStack is doing in the background:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:"function GlobalLoadingIndicator() {\n  const isFetching = useIsFetching();\n  return isFetching ? <div style={styles.globalLoadingIndicator}></div> : null;\n}\n"})}),"\n",(0,r.jsxs)(t.p,{children:["For more details on advanced usage of TanStack Query hooks, see the ",(0,r.jsx)(t.a,{href:"https://tanstack.com/query/latest/docs/react/guides/mutations",children:"TanStack documentation"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["The following example demonstrates how to use the state returned by TanStack to render a loading indicator while a mutation is in progress, and an error message if the mutation fails. For additional examples, see the ",(0,r.jsx)(t.a,{href:"#complete-example",children:"Complete Example"})," below."]}),"\n",(0,r.jsxs)(i,{children:[(0,r.jsx)(s,{name:"TypeScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:"<>\n  {updateMutation.isError &&\n  updateMutation.error instanceof Error ? (\n    <div>An error occurred: {updateMutation.error.message}</div>\n  ) : null}\n\n  {updateMutation.isSuccess ? (\n    <div>Real Estate Property updated!</div>\n  ) : null}\n\n  <button\n    onClick={() =>\n      updateMutation.mutate({\n        id: realEstateProperty.id,\n        address: `${Math.floor(\n          1000 + Math.random() * 9000\n        )} Main St`,\n      })\n    }\n  >\n    Update Address\n  </button>\n</>\n"})})}),(0,r.jsx)(s,{name:"JavaScript",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:"<>\n  {updateMutation.isError ? (\n    <div>An error occurred: {updateMutation.error.message}</div>\n  ) : null}\n\n  {updateMutation.isSuccess ? (\n    <div>Real Estate Property updated!</div>\n  ) : null}\n\n  <button\n    onClick={() =>\n      updateMutation.mutate({\n        id: realEstateProperty.id,\n        address: `${Math.floor(\n          1000 + Math.random() * 9000\n        )} Main St`,\n      })\n    }\n  >\n    Update Address\n  </button>\n</>\n"})})})]}),"\n",(0,r.jsx)(t.h2,{id:"complete-example",children:"Complete example"}),"\n",(0,r.jsxs)(i,{children:[(0,r.jsxs)(s,{name:"TypeScript",children:[(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-ts",children:'// index.tsx:\nimport React from "react";\nimport ReactDOM from "react-dom/client";\nimport "./index.css";\nimport App from "./App";\nimport { Amplify } from "aws-amplify";\nimport config from "./amplifyconfiguration.json";\nimport { QueryClient, QueryClientProvider } from "@tanstack/react-query";\nimport { ReactQueryDevtools } from "@tanstack/react-query-devtools";\nimport "@aws-amplify/ui-react/styles.css";\n\nAmplify.configure(config);\n\n// Create a client\nconst queryClient = new QueryClient();\n\nconst root = ReactDOM.createRoot(\n  document.getElementById("root") as HTMLElement\n);\n\n// Provide the client to your App\nroot.render(\n  <React.StrictMode>\n    <QueryClientProvider client={queryClient}>\n      <App />\n      <ReactQueryDevtools initialIsOpen={false} />\n    </QueryClientProvider>\n  </React.StrictMode>\n);\n'})}),(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-tsx",children:'// App.tsx:\nimport { useState } from "react";\nimport { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";\nimport { useIsFetching } from "@tanstack/react-query";\nimport { generateClient } from \'aws-amplify/api\'\nimport * as mutations from "./graphql/mutations";\nimport * as queries from "./graphql/queries";\nimport { CreateRealEstatePropertyInput, DeleteRealEstatePropertyInput, RealEstateProperty, UpdateRealEstatePropertyInput } from "./API";\n\n/**\n * https://www.tanstack.com/query/v4/docs/react/guides/background-fetching-indicators#displaying-global-background-fetching-loading-state\n * For the purposes of this demo, we show a global loading indicator when *any*\n * queries are fetching (including in the background) in order to help visualize\n * what TanStack is doing in the background. This example also displays\n * indicators for individual query and mutation loading states.\n */\nfunction GlobalLoadingIndicator() {\n  const isFetching = useIsFetching();\n\n  return isFetching ? <div style={styles.globalLoadingIndicator}></div> : null;\n}\n\nconst client = generateClient()\n\nfunction App() {\n  const [currentRealEstatePropertyId, setCurrentRealEstatePropertyId] =\n    useState<string | null>(null);\n\n  // Access the client\n  const queryClient = useQueryClient();\n\n  // TanStack Query for listing all real estate properties:\n  const {\n    data: realEstateProperties,\n    isLoading,\n    isSuccess,\n    isError: isErrorQuery,\n  } = useQuery({\n    queryKey: ["realEstateProperties"],\n    queryFn: async () => {\n      const response = await client.graphql({\n        query: queries.listRealEstateProperties,\n      });\n\n      const allRealEstateProperties =\n        response?.data?.listRealEstateProperties?.items;\n\n      if (!allRealEstateProperties) return null;\n\n      return allRealEstateProperties;\n    },\n  });\n\n  // TanStack create mutation with optimistic updates\n  const createMutation = useMutation({\n    mutationFn: async (realEstatePropertyDetails: CreateRealEstatePropertyInput) => {\n      const response = await client.graphql({\n        query: mutations.createRealEstateProperty,\n        variables: { input: realEstatePropertyDetails },\n      });\n\n      const newRealEstateProperty = response?.data?.createRealEstateProperty;\n      return newRealEstateProperty;\n    },\n    // When mutate is called:\n    onMutate: async (newRealEstateProperty) => {\n      // Cancel any outgoing refetches\n      // (so they don\'t overwrite our optimistic update)\n      await queryClient.cancelQueries({ queryKey: ["realEstateProperties"] });\n\n      // Snapshot the previous value\n      const previousRealEstateProperties = queryClient.getQueryData([\n        "realEstateProperties",\n      ]);\n\n      // Optimistically update to the new value\n      if (previousRealEstateProperties) {\n        queryClient.setQueryData(["realEstateProperties"], (old: RealEstateProperty[]) => [\n          ...old,\n          newRealEstateProperty,\n        ]);\n      }\n\n      // Return a context object with the snapshotted value\n      return { previousRealEstateProperties };\n    },\n    // If the mutation fails,\n    // use the context returned from onMutate to rollback\n    onError: (err, newRealEstateProperty, context) => {\n      console.error("Error saving record:", err, newRealEstateProperty);\n      if (context?.previousRealEstateProperties) {\n        queryClient.setQueryData(\n          ["realEstateProperties"],\n          context.previousRealEstateProperties\n        );\n      }\n    },\n    // Always refetch after error or success:\n    onSettled: () => {\n      queryClient.invalidateQueries({ queryKey: ["realEstateProperties"] });\n    },\n  });\n\n  /**\n   * Note: this example does not return to the list view on delete in order\n   * to demonstrate the optimistic update.\n   */\n  function RealEstatePropertyDetailView() {\n    const {\n      data: realEstateProperty,\n      isLoading,\n      isSuccess,\n      isError: isErrorQuery,\n    } = useQuery({\n      queryKey: ["realEstateProperties", currentRealEstatePropertyId],\n      queryFn: async () => {\n        if (!currentRealEstatePropertyId) { return }\n\n        const response = await client.graphql({\n          query: queries.getRealEstateProperty,\n          variables: { id: currentRealEstatePropertyId },\n        });\n\n        return response.data?.getRealEstateProperty;\n      },\n    });\n\n    // TanStack update mutation with optimistic updates\n    const updateMutation = useMutation({\n      mutationFn: async (\n        realEstatePropertyDetails: UpdateRealEstatePropertyInput\n      ) => {\n        const response = await client.graphql({\n          query: mutations.updateRealEstateProperty,\n          variables: { input: realEstatePropertyDetails },\n        });\n\n        return response?.data?.updateRealEstateProperty;\n      },\n      // When mutate is called:\n      onMutate: async (newRealEstateProperty) => {\n        // Cancel any outgoing refetches\n        // (so they don\'t overwrite our optimistic update)\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties", newRealEstateProperty.id],\n        });\n\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties"],\n        });\n\n        // Snapshot the previous value\n        const previousRealEstateProperty = queryClient.getQueryData([\n          "realEstateProperties",\n          newRealEstateProperty.id,\n        ]);\n\n        // Optimistically update to the new value\n        if (previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", newRealEstateProperty.id],\n            /**\n             * `newRealEstateProperty` will at first only include updated values for\n             * the record. To avoid only rendering optimistic values for updated\n             * fields on the UI, include the previous values for all fields:\n             */\n            { ...previousRealEstateProperty, ...newRealEstateProperty }\n          );\n        }\n\n        // Return a context with the previous and new realEstateProperty\n        return { previousRealEstateProperty, newRealEstateProperty };\n      },\n      // If the mutation fails, use the context we returned above\n      onError: (err, newRealEstateProperty, context) => {\n        console.error("Error updating record:", err, newRealEstateProperty);\n        if (context?.previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", context.newRealEstateProperty.id],\n            context.previousRealEstateProperty\n          );\n        }\n      },\n      // Always refetch after error or success:\n      onSettled: (newRealEstateProperty) => {\n        if (newRealEstateProperty) {\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties", newRealEstateProperty.id],\n          });\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties"],\n          });\n        }\n      },\n    });\n\n    // TanStack delete mutation with optimistic updates\n    const deleteMutation = useMutation({\n      mutationFn: async (\n        realEstatePropertyDetails: DeleteRealEstatePropertyInput\n      ) => {\n        const response = await client.graphql({\n          query: mutations.deleteRealEstateProperty,\n          variables: { input: realEstatePropertyDetails },\n        });\n\n        return response?.data?.deleteRealEstateProperty;\n      },\n      // When mutate is called:\n      onMutate: async (newRealEstateProperty) => {\n        // Cancel any outgoing refetches\n        // (so they don\'t overwrite our optimistic update)\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties", newRealEstateProperty.id],\n        });\n\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties"],\n        });\n\n        // Snapshot the previous value\n        const previousRealEstateProperty = queryClient.getQueryData([\n          "realEstateProperties",\n          newRealEstateProperty.id,\n        ]);\n\n        // Optimistically update to the new value\n        if (previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", newRealEstateProperty.id],\n            newRealEstateProperty\n          );\n        }\n\n        // Return a context with the previous and new realEstateProperty\n        return { previousRealEstateProperty, newRealEstateProperty };\n      },\n      // If the mutation fails, use the context we returned above\n      onError: (err, newRealEstateProperty, context) => {\n        console.error("Error deleting record:", err, newRealEstateProperty);\n        if (context?.previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", context.newRealEstateProperty.id],\n            context.previousRealEstateProperty\n          );\n        }\n      },\n      // Always refetch after error or success:\n      onSettled: (newRealEstateProperty) => {\n        if (newRealEstateProperty) {\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties", newRealEstateProperty.id],\n          });\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties"],\n          });\n        }\n      },\n    });\n\n    return (\n      <div style={styles.detailViewContainer}>\n        <h2>Real Estate Property Detail View</h2>\n        {isErrorQuery && <div>{"Problem loading Real Estate Property"}</div>}\n        {isLoading && (\n          <div style={styles.loadingIndicator}>\n            {"Loading Real Estate Property..."}\n          </div>\n        )}\n        {isSuccess && (\n          <div>\n            <p>{`Name: ${realEstateProperty?.name}`}</p>\n            <p>{`Address: ${realEstateProperty?.address}`}</p>\n          </div>\n        )}\n        {realEstateProperty && (\n          <div>\n            <div>\n              {updateMutation.isPending ? (\n                "Updating Real Estate Property..."\n              ) : (\n                <>\n                  {updateMutation.isError &&\n                  updateMutation.error instanceof Error ? (\n                    <div>An error occurred: {updateMutation.error.message}</div>\n                  ) : null}\n\n                  {updateMutation.isSuccess ? (\n                    <div>Real Estate Property updated!</div>\n                  ) : null}\n\n                  <button\n                    onClick={() =>\n                      updateMutation.mutate({\n                        id: realEstateProperty.id,\n                        name: `Updated Home ${Date.now()}`,\n                      })\n                    }\n                  >\n                    Update Name\n                  </button>\n                  <button\n                    onClick={() =>\n                      updateMutation.mutate({\n                        id: realEstateProperty.id,\n                        address: `${Math.floor(\n                          1000 + Math.random() * 9000\n                        )} Main St`,\n                      })\n                    }\n                  >\n                    Update Address\n                  </button>\n                </>\n              )}\n            </div>\n\n            <div>\n              {deleteMutation.isPending ? (\n                "Deleting Real Estate Property..."\n              ) : (\n                <>\n                  {deleteMutation.isError &&\n                  deleteMutation.error instanceof Error ? (\n                    <div>An error occurred: {deleteMutation.error.message}</div>\n                  ) : null}\n\n                  {deleteMutation.isSuccess ? (\n                    <div>Real Estate Property deleted!</div>\n                  ) : null}\n\n                  <button\n                    onClick={() =>\n                      deleteMutation.mutate({\n                        id: realEstateProperty.id,\n                      })\n                    }\n                  >\n                    Delete\n                  </button>\n                </>\n              )}\n            </div>\n          </div>\n        )}\n        <button onClick={() => setCurrentRealEstatePropertyId(null)}>\n          Back\n        </button>\n      </div>\n    );\n  }\n\n  return (\n    <div>\n      {!currentRealEstatePropertyId && (\n        <div style={styles.appContainer}>\n          <h1>Real Estate Properties:</h1>\n          <div>\n            {createMutation.isPending ? (\n              "Adding Real Estate Property..."\n            ) : (\n              <>\n                {createMutation.isError &&\n                createMutation.error instanceof Error ? (\n                  <div>An error occurred: {createMutation.error.message}</div>\n                ) : null}\n\n                {createMutation.isSuccess ? (\n                  <div>Real Estate Property added!</div>\n                ) : null}\n\n                <button\n                  onClick={() => {\n                    createMutation.mutate({\n                      name: `New Home ${Date.now()}`,\n                      address: `${Math.floor(\n                        1000 + Math.random() * 9000\n                      )} Main St`,\n                    });\n                  }}\n                >\n                  Add RealEstateProperty\n                </button>\n              </>\n            )}\n          </div>\n          <ul style={styles.propertiesList}>\n            {isLoading && (\n              <div style={styles.loadingIndicator}>\n                {"Loading Real Estate Properties..."}\n              </div>\n            )}\n            {isErrorQuery && (\n              <div>{"Problem loading Real Estate Properties"}</div>\n            )}\n            {isSuccess &&\n              realEstateProperties?.map((realEstateProperty, idx) => {\n                if (!realEstateProperty) return null;\n                return (\n                  <li\n                    style={styles.listItem}\n                    key={`${idx}-${realEstateProperty.id}`}\n                  >\n                    <p>{realEstateProperty.name}</p>\n                    <button\n                      style={styles.detailViewButton}\n                      onClick={() =>\n                        setCurrentRealEstatePropertyId(realEstateProperty.id)\n                      }\n                    >\n                      Detail View\n                    </button>\n                  </li>\n                );\n              })}\n          </ul>\n        </div>\n      )}\n      {currentRealEstatePropertyId && <RealEstatePropertyDetailView />}\n      <GlobalLoadingIndicator />\n    </div>\n  );\n}\n\nexport default App;\n\nconst styles = {\n  appContainer: {\n    display: "flex",\n    flexDirection: "column",\n    alignItems: "center",\n  },\n  detailViewButton: { marginLeft: "1rem" },\n  detailViewContainer: { border: "1px solid black", padding: "3rem" },\n  globalLoadingIndicator: {\n    position: "fixed",\n    top: 0,\n    left: 0,\n    width: "100%",\n    height: "100%",\n    border: "4px solid blue",\n    pointerEvents: "none",\n  },\n  listItem: {\n    display: "flex",\n    justifyContent: "space-between",\n    border: "1px dotted grey",\n    padding: ".5rem",\n    margin: ".1rem",\n  },\n  loadingIndicator: {\n    border: "1px solid black",\n    padding: "1rem",\n    margin: "1rem",\n  },\n  propertiesList: {\n    display: "flex",\n    flexDirection: "column",\n    alignItems: "center",\n    justifyContent: "start",\n    width: "50%",\n    border: "1px solid black",\n    padding: "1rem",\n    listStyleType: "none",\n  },\n} as const;\n'})})]}),(0,r.jsxs)(s,{name:"JavaScript",children:[(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:'// index.jsx\nimport React from "react";\nimport ReactDOM from "react-dom/client";\nimport "./index.css";\nimport App from "./App";\nimport { Amplify } from "aws-amplify";\nimport config from "./amplifyconfiguration.json";\nimport { QueryClient, QueryClientProvider } from "@tanstack/react-query";\nimport { ReactQueryDevtools } from "@tanstack/react-query-devtools";\nimport "@aws-amplify/ui-react/styles.css";\n\nAmplify.configure(config);\n\n// Create a client\nconst queryClient = new QueryClient();\n\nconst root = ReactDOM.createRoot(document.getElementById("root"));\n\n// Provide the client to your App\nroot.render(\n  <React.StrictMode>\n    <QueryClientProvider client={queryClient}>\n      <App />\n      <ReactQueryDevtools initialIsOpen={false} />\n    </QueryClientProvider>\n  </React.StrictMode>\n);\n'})}),(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-js",children:'// App.jsx:\nimport { useState } from "react";\nimport { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";\nimport { useIsFetching } from "@tanstack/react-query";\nimport { generateClient } from \'aws-amplify/api\'\nimport * as mutations from "./graphql/mutations";\nimport * as queries from "./graphql/queries";\n\n/**\n * https://www.tanstack.com/query/v4/docs/react/guides/background-fetching-indicators#displaying-global-background-fetching-loading-state\n * For the purposes of this demo, we show a global loading indicator when *any*\n * queries are fetching (including in the background) in order to help visualize\n * what TanStack is doing in the background. This example also displays\n * indicators for individual query and mutation loading states.\n */\nfunction GlobalLoadingIndicator() {\n  const isFetching = useIsFetching();\n\n  return isFetching ? <div style={styles.globalLoadingIndicator}></div> : null;\n}\n\nconst client = generateClient()\n\nfunction App() {\n  const [currentRealEstatePropertyId, setCurrentRealEstatePropertyId] = useState(null);\n\n  // Access the client\n  const queryClient = useQueryClient();\n\n  // TanStack Query for listing all real estate properties:\n  const {\n    data: realEstateProperties,\n    isLoading,\n    isSuccess,\n    isError: isErrorQuery,\n  } = useQuery({\n    queryKey: ["realEstateProperties"],\n    queryFn: async () => {\n      const response = await client.graphql({\n        query: queries.listRealEstateProperties,\n      });\n\n      const allRealEstateProperties =\n        response?.data?.listRealEstateProperties?.items;\n\n      if (!allRealEstateProperties) return null;\n\n      return allRealEstateProperties;\n    },\n  });\n\n  // TanStack create mutation with optimistic updates\n  const createMutation = useMutation({\n    mutationFn: async (realEstatePropertyDetails) => {\n      const response = await client.graphql({\n        query: mutations.createRealEstateProperty,\n        variables: { input: realEstatePropertyDetails },\n      });\n\n      const newRealEstateProperty = response?.data?.createRealEstateProperty;\n      return newRealEstateProperty;\n    },\n    // When mutate is called:\n    onMutate: async (newRealEstateProperty) => {\n      // Cancel any outgoing refetches\n      // (so they don\'t overwrite our optimistic update)\n      await queryClient.cancelQueries({ queryKey: ["realEstateProperties"] });\n\n      // Snapshot the previous value\n      const previousRealEstateProperties = queryClient.getQueryData([\n        "realEstateProperties",\n      ]);\n\n      // Optimistically update to the new value\n      if (previousRealEstateProperties) {\n        queryClient.setQueryData(["realEstateProperties"], (old) => [\n          ...old,\n          newRealEstateProperty,\n        ]);\n      }\n\n      // Return a context object with the snapshotted value\n      return { previousRealEstateProperties };\n    },\n    // If the mutation fails,\n    // use the context returned from onMutate to rollback\n    onError: (err, newRealEstateProperty, context) => {\n      console.error("Error saving record:", err, newRealEstateProperty);\n      if (context?.previousRealEstateProperties) {\n        queryClient.setQueryData(\n          ["realEstateProperties"],\n          context.previousRealEstateProperties\n        );\n      }\n    },\n    // Always refetch after error or success:\n    onSettled: () => {\n      queryClient.invalidateQueries({ queryKey: ["realEstateProperties"] });\n    },\n  });\n\n  /**\n   * Note: this example does not return to the list view on delete in order\n   * to demonstrate the optimistic update.\n   */\n  function RealEstatePropertyDetailView() {\n    const {\n      data: realEstateProperty,\n      isLoading,\n      isSuccess,\n      isError: isErrorQuery,\n    } = useQuery({\n      queryKey: ["realEstateProperties", currentRealEstatePropertyId],\n      queryFn: async () => {\n        if (!currentRealEstatePropertyId) { return }\n\n        const response = await client.graphql({\n          query: queries.getRealEstateProperty,\n          variables: { id: currentRealEstatePropertyId },\n        });\n\n        return response.data?.getRealEstateProperty;\n      },\n    });\n\n    // TanStack update mutation with optimistic updates\n    const updateMutation = useMutation({\n      mutationFn: async (realEstatePropertyDetails) => {\n        const response = await client.graphql({\n          query: mutations.updateRealEstateProperty,\n          variables: { input: realEstatePropertyDetails },\n        });\n\n        return response?.data?.updateRealEstateProperty;\n      },\n      // When mutate is called:\n      onMutate: async (newRealEstateProperty) => {\n        // Cancel any outgoing refetches\n        // (so they don\'t overwrite our optimistic update)\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties", newRealEstateProperty.id],\n        });\n\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties"],\n        });\n\n        // Snapshot the previous value\n        const previousRealEstateProperty = queryClient.getQueryData([\n          "realEstateProperties",\n          newRealEstateProperty.id,\n        ]);\n\n        // Optimistically update to the new value\n        if (previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", newRealEstateProperty.id],\n            /**\n             * `newRealEstateProperty` will at first only include updated values for\n             * the record. To avoid only rendering optimistic values for updated\n             * fields on the UI, include the previous values for all fields:\n             */\n            { ...previousRealEstateProperty, ...newRealEstateProperty }\n          );\n        }\n\n        // Return a context with the previous and new realEstateProperty\n        return { previousRealEstateProperty, newRealEstateProperty };\n      },\n      // If the mutation fails, use the context we returned above\n      onError: (err, newRealEstateProperty, context) => {\n        console.error("Error updating record:", err, newRealEstateProperty);\n        if (context?.previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", context.newRealEstateProperty.id],\n            context.previousRealEstateProperty\n          );\n        }\n      },\n      // Always refetch after error or success:\n      onSettled: (newRealEstateProperty) => {\n        if (newRealEstateProperty) {\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties", newRealEstateProperty.id],\n          });\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties"],\n          });\n        }\n      },\n    });\n\n    // TanStack delete mutation with optimistic updates\n    const deleteMutation = useMutation({\n      mutationFn: async (realEstatePropertyDetails) => {\n        const response = await client.graphql({\n          query: mutations.deleteRealEstateProperty,\n          variables: { input: realEstatePropertyDetails },\n        });\n\n        return response?.data?.deleteRealEstateProperty;\n      },\n      // When mutate is called:\n      onMutate: async (newRealEstateProperty) => {\n        // Cancel any outgoing refetches\n        // (so they don\'t overwrite our optimistic update)\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties", newRealEstateProperty.id],\n        });\n\n        await queryClient.cancelQueries({\n          queryKey: ["realEstateProperties"],\n        });\n\n        // Snapshot the previous value\n        const previousRealEstateProperty = queryClient.getQueryData([\n          "realEstateProperties",\n          newRealEstateProperty.id,\n        ]);\n\n        // Optimistically update to the new value\n        if (previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", newRealEstateProperty.id],\n            newRealEstateProperty\n          );\n        }\n\n        // Return a context with the previous and new realEstateProperty\n        return { previousRealEstateProperty, newRealEstateProperty };\n      },\n      // If the mutation fails, use the context we returned above\n      onError: (err, newRealEstateProperty, context) => {\n        console.error("Error deleting record:", err, newRealEstateProperty);\n        if (context?.previousRealEstateProperty) {\n          queryClient.setQueryData(\n            ["realEstateProperties", context.newRealEstateProperty.id],\n            context.previousRealEstateProperty\n          );\n        }\n      },\n      // Always refetch after error or success:\n      onSettled: (newRealEstateProperty) => {\n        if (newRealEstateProperty) {\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties", newRealEstateProperty.id],\n          });\n          queryClient.invalidateQueries({\n            queryKey: ["realEstateProperties"],\n          });\n        }\n      },\n    });\n\n    return (\n      <div style={styles.detailViewContainer}>\n        <h2>Real Estate Property Detail View</h2>\n        {isErrorQuery && <div>{"Problem loading Real Estate Property"}</div>}\n        {isLoading && (\n          <div style={styles.loadingIndicator}>\n            {"Loading Real Estate Property..."}\n          </div>\n        )}\n        {isSuccess && (\n          <div>\n            <p>{`Name: ${realEstateProperty?.name}`}</p>\n            <p>{`Address: ${realEstateProperty?.address}`}</p>\n          </div>\n        )}\n        {realEstateProperty && (\n          <div>\n            <div>\n              {updateMutation.isPending ? (\n                "Updating Real Estate Property..."\n              ) : (\n                <>\n                  {updateMutation.isError &&\n                  updateMutation.error instanceof Error ? (\n                    <div>An error occurred: {updateMutation.error.message}</div>\n                  ) : null}\n\n                  {updateMutation.isSuccess ? (\n                    <div>Real Estate Property updated!</div>\n                  ) : null}\n\n                  <button\n                    onClick={() =>\n                      updateMutation.mutate({\n                        id: realEstateProperty.id,\n                        name: `Updated Home ${Date.now()}`,\n                      })\n                    }\n                  >\n                    Update Name\n                  </button>\n                  <button\n                    onClick={() =>\n                      updateMutation.mutate({\n                        id: realEstateProperty.id,\n                        address: `${Math.floor(\n                          1000 + Math.random() * 9000\n                        )} Main St`,\n                      })\n                    }\n                  >\n                    Update Address\n                  </button>\n                </>\n              )}\n            </div>\n\n            <div>\n              {deleteMutation.isPending ? (\n                "Deleting Real Estate Property..."\n              ) : (\n                <>\n                  {deleteMutation.isError &&\n                  deleteMutation.error instanceof Error ? (\n                    <div>An error occurred: {deleteMutation.error.message}</div>\n                  ) : null}\n\n                  {deleteMutation.isSuccess ? (\n                    <div>Real Estate Property deleted!</div>\n                  ) : null}\n\n                  <button\n                    onClick={() =>\n                      deleteMutation.mutate({\n                        id: realEstateProperty.id,\n                      })\n                    }\n                  >\n                    Delete\n                  </button>\n                </>\n              )}\n            </div>\n          </div>\n        )}\n        <button onClick={() => setCurrentRealEstatePropertyId(null)}>\n          Back\n        </button>\n      </div>\n    );\n  }\n\n  return (\n    <div>\n      {!currentRealEstatePropertyId && (\n        <div style={styles.appContainer}>\n          <h1>Real Estate Properties:</h1>\n          <div>\n            {createMutation.isPending ? (\n              "Adding Real Estate Property..."\n            ) : (\n              <>\n                {createMutation.isError &&\n                createMutation.error instanceof Error ? (\n                  <div>An error occurred: {createMutation.error.message}</div>\n                ) : null}\n\n                {createMutation.isSuccess ? (\n                  <div>Real Estate Property added!</div>\n                ) : null}\n\n                <button\n                  onClick={() => {\n                    createMutation.mutate({\n                      name: `New Home ${Date.now()}`,\n                      address: `${Math.floor(\n                        1000 + Math.random() * 9000\n                      )} Main St`,\n                    });\n                  }}\n                >\n                  Add RealEstateProperty\n                </button>\n              </>\n            )}\n          </div>\n          <ul style={styles.propertiesList}>\n            {isLoading && (\n              <div style={styles.loadingIndicator}>\n                {"Loading Real Estate Properties..."}\n              </div>\n            )}\n            {isErrorQuery && (\n              <div>{"Problem loading Real Estate Properties"}</div>\n            )}\n            {isSuccess &&\n              realEstateProperties?.map((realEstateProperty, idx) => {\n                if (!realEstateProperty) return null;\n                return (\n                  <li\n                    style={styles.listItem}\n                    key={`${idx}-${realEstateProperty.id}`}\n                  >\n                    <p>{realEstateProperty.name}</p>\n                    <button\n                      style={styles.detailViewButton}\n                      onClick={() =>\n                        setCurrentRealEstatePropertyId(realEstateProperty.id)\n                      }\n                    >\n                      Detail View\n                    </button>\n                  </li>\n                );\n              })}\n          </ul>\n        </div>\n      )}\n      {currentRealEstatePropertyId && <RealEstatePropertyDetailView />}\n      <GlobalLoadingIndicator />\n    </div>\n  );\n}\n\nexport default App;\n\nconst styles = {\n  appContainer: {\n    display: "flex",\n    flexDirection: "column",\n    alignItems: "center",\n  },\n  detailViewButton: { marginLeft: "1rem" },\n  detailViewContainer: { border: "1px solid black", padding: "3rem" },\n  globalLoadingIndicator: {\n    position: "fixed",\n    top: 0,\n    left: 0,\n    width: "100%",\n    height: "100%",\n    border: "4px solid blue",\n    pointerEvents: "none",\n  },\n  listItem: {\n    display: "flex",\n    justifyContent: "space-between",\n    border: "1px dotted grey",\n    padding: ".5rem",\n    margin: ".1rem",\n  },\n  loadingIndicator: {\n    border: "1px solid black",\n    padding: "1rem",\n    margin: "1rem",\n  },\n  propertiesList: {\n    display: "flex",\n    flexDirection: "column",\n    alignItems: "center",\n    justifyContent: "start",\n    width: "50%",\n    border: "1px solid black",\n    padding: "1rem",\n    listStyleType: "none",\n  },\n};\n'})})]})]})]})}let s=function(e={}){let{wrapper:t}=Object.assign({},(0,a.R)(),e.components);return t?(0,r.jsx)(t,Object.assign({},e,{children:(0,r.jsx)(i,e)})):i(e)};function o(e,t){throw Error("Expected "+(t?"component":"object")+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}function l(e){let t=Object.assign({p:"p",a:"a",pre:"pre",code:"code",ul:"ul",li:"li",h2:"h2"},(0,a.R)(),e.components),{Callout:n,BlockSwitcher:i,Block:s}=t;return s||d("Block",!0),i||d("BlockSwitcher",!0),n||d("Callout",!0),(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(t.p,{children:"Implementing optimistic UI with GraphQL API category allows CRUD operations to be rendered immediately on the UI before the request roundtrip has completed, and allows you to rollback changes on the UI when API calls are unsuccessful."}),"\n",(0,r.jsx)(t.p,{children:"In the following example, we'll create a list view that optimistically renders newly created items, updates and deletes."}),"\n",(0,r.jsx)(n,{children:(0,r.jsxs)(t.p,{children:["For more on the Amplify GraphQL API, see the ",(0,r.jsx)(t.a,{href:"/gen1/%5Bplatform%5D/build-a-backend/graphqlapi/set-up-graphql-api/",children:"API documentation"}),"."]})}),"\n",(0,r.jsx)(t.p,{children:"To get started, go to your project directory and run the command:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-bash",children:"amplify add api\n"})}),"\n",(0,r.jsx)(t.p,{children:"Choose the following when prompted:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-bash",children:"? Select from one of the below mentioned services: `GraphQL`\n? Here is the GraphQL API that we will create. Select a setting to edit or continue Authorization modes: `API key (default, expiration time: 7 days from now)`\n? Choose the default authorization type for the API `API key`\n✔ Enter a description for the API key: \xb7 \n✔ After how many days from now the API key should expire (1-365): \xb7 `365`\n? Configure additional auth types? `No`\n? Here is the GraphQL API that we will create. Select a setting to edit or continue `Continue`\n? Choose a schema template: `Blank Schema`\n"})}),"\n",(0,r.jsxs)(t.p,{children:["The schema file can also be found under ",(0,r.jsx)(t.code,{children:"amplify/backend/api/[name of project]/schema.graphql"}),". Replace it with the following contents:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-graphql",children:"type RealEstateProperty @model @auth(rules: [{ allow: public }]) {\n  id: ID!\n  name: String!\n  address: String\n}\n"})}),"\n",(0,r.jsxs)(t.p,{children:["Save the schema and run ",(0,r.jsx)(t.code,{children:"amplify push"})," to deploy the changes. For the purposes of this guide, we'll build a Real Estate Property listing application."]}),"\n",(0,r.jsxs)(t.p,{children:["Once the backend has been provisioned, run ",(0,r.jsx)(t.code,{children:"amplify codegen models"})," to generate the Swift model types for the app."]}),"\n",(0,r.jsxs)(t.p,{children:["Next, add the Amplify(",(0,r.jsx)(t.code,{children:"https://github.com/aws-amplify/amplify-swift.git"}),") package to your Xcode project and select the following modules to import when prompted:"]}),"\n",(0,r.jsxs)(t.ul,{children:["\n",(0,r.jsx)(t.li,{children:"AWSAPIPlugin"}),"\n",(0,r.jsx)(t.li,{children:"AWSCognitoAuthPlugin"}),"\n",(0,r.jsx)(t.li,{children:"AWSS3StoragePlugin"}),"\n",(0,r.jsx)(t.li,{children:"Amplify"}),"\n"]}),"\n",(0,r.jsx)(n,{children:(0,r.jsxs)(t.p,{children:["For the complete working example see the ",(0,r.jsx)(t.a,{href:"#complete-example",children:"Complete Example"})," below."]})}),"\n",(0,r.jsx)(t.h2,{id:"how-to-use-a-swift-actor-to-perform-optimistic-ui-updates",children:"How to use a Swift Actor to perform optimistic UI updates"}),"\n",(0,r.jsx)(t.p,{children:"A Swift actor serializes access to its underlying properties. In this example, the actor will hold a list of items that will be published to the UI through a Combine publisher whenever the list is accessed. On a high level, the methods on the actor will perform the following:"}),"\n",(0,r.jsxs)(t.ul,{children:["\n",(0,r.jsx)(t.li,{children:"create a new model, add it to the list, remove the newly added item from the list if the API request is unsuccessful"}),"\n",(0,r.jsx)(t.li,{children:"update the existing model in the list, revert the update on the list if the API request is unsuccessful"}),"\n",(0,r.jsx)(t.li,{children:"delete the existing model from the list, add the item back into the list if the API request is unsuccessful"}),"\n"]}),"\n",(0,r.jsx)(t.p,{children:"By providing these methods through an actor object, the underlying list will be accessed serially so that the entire operation can be rolled back if needed."}),"\n",(0,r.jsx)(t.p,{children:"To create an actor object that allows optimistic UI updates, create a new file and add the following code."}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'import Amplify\nimport SwiftUI\nimport Combine\n\nactor RealEstatePropertyList {\n    \n    private var properties: [RealEstateProperty?] = [] {\n        didSet {\n            subject.send(properties.compactMap { $0 })\n        }\n    }\n    \n    private let subject = PassthroughSubject<[RealEstateProperty], Never>()\n    var publisher: AnyPublisher<[RealEstateProperty], Never> {\n        subject.eraseToAnyPublisher()\n    }\n    \n    func listProperties() async throws {\n        let result = try await Amplify.API.query(request: .list(RealEstateProperty.self))\n        guard case .success(let propertyList) = result else {\n            print("Failed with error: ", result)\n            return\n        }\n        properties = propertyList.elements\n    }\n}\n'})}),"\n",(0,r.jsxs)(t.p,{children:["Calling the ",(0,r.jsx)(t.code,{children:"listProperties()"})," method will perform a query with GraphQL API and store the results in the ",(0,r.jsx)(t.code,{children:"properties"})," property. When this property is set, the list is sent back to the subscribers. In your UI, create a view model and subscribe to updates:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'class RealEstatePropertyContainerViewModel: ObservableObject {\n    @Published var properties: [RealEstateProperty] = []\n    var sink: AnyCancellable?\n    \n    var propertyList = RealEstatePropertyList()\n    init() {\n        Task {\n            sink = await propertyList.publisher\n                .receive(on: DispatchQueue.main)\n                .sink { properties in\n                    print("Updating property list")\n                    self.properties = properties\n            }\n        }\n    }\n    \n    func loadList() {\n        Task {\n            try? await propertyList.listProperties()\n        }\n    }\n}\n\nstruct RealEstatePropertyContainerView: View {\n    @StateObject var vm = RealEstatePropertyContainerViewModel()\n    @State private var propertyName: String = ""\n    \n    var body: some View {\n        Text("Hello")\n    }\n}\n'})}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-rendering-a-newly-created-record",children:"Optimistically rendering a newly created record"}),"\n",(0,r.jsxs)(t.p,{children:["To optimistically render a newly created record returned from the GraphQL API, add a method to the ",(0,r.jsx)(t.code,{children:"actor RealEstatePropertyList"}),":"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'func createProperty(name: String, address: String? = nil) {\n    let property = RealEstateProperty(name: name, address: address)\n    // Optimistically send the newly created property, for the UI to render.\n    properties.append(property)\n\n    Task {\n        do {\n            // Create the property record\n            let result = try await Amplify.API.mutate(request: .create(property))\n            guard case .failure(let graphQLResponse) = result else {\n                return\n            }\n            print("Failed with error: ", graphQLResponse)\n            // Remove the newly created property\n            if let index = properties.firstIndex(where: { $0?.id == property.id }) {\n                properties.remove(at: index)\n            }\n        } catch {\n            print("Failed with error: ", error)\n            // Remove the newly created property\n            if let index = properties.firstIndex(where: { $0?.id == property.id }) {\n                properties.remove(at: index)\n            }\n        }\n    }\n}\n'})}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-rendering-a-record-update",children:"Optimistically rendering a record update"}),"\n",(0,r.jsx)(t.p,{children:"To optimistically render updates on a single item, use the code snippet like below:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'func updateProperty(_ property: RealEstateProperty) async {\n    guard let index = properties.firstIndex(where: { $0?.id == property.id }) else {\n        print("No property to update")\n        return\n    }\n\n    // Optimistically update the property, for the UI to render.\n    let rollbackProperty = properties[index]\n    properties[index] = property\n    \n    do {\n        // Update the property record\n        let result = try await Amplify.API.mutate(request: .update(property))\n        guard case .failure(let graphQLResponse) = result else {\n            return\n        }\n        print("Failed with error: ", graphQLResponse)\n        properties[index] = rollbackProperty\n    } catch {\n        print("Failed with error: ", error)\n        properties[index] = rollbackProperty\n    }\n}\n'})}),"\n",(0,r.jsx)(t.h2,{id:"optimistically-render-deleting-a-record",children:"Optimistically render deleting a record"}),"\n",(0,r.jsx)(t.p,{children:"To optimistically render a GraphQL API delete, use the code snippet like below:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'func deleteProperty(_ property: RealEstateProperty) async {\n  guard let index = properties.firstIndex(where: { $0?.id == property.id }) else {\n      print("No property to remove")\n      return\n  }\n  \n  // Optimistically remove the property, for the UI to render.\n  let rollbackProperty = properties[index]\n  properties[index] = nil\n  \n  do {\n      // Delete the property record\n      let result = try await Amplify.API.mutate(request: .delete(property))\n      switch result {\n      case .success:\n          // Finalize the removal\n          properties.remove(at: index)\n      case .failure(let graphQLResponse):\n          print("Failed with error: ", graphQLResponse)\n          // Undo the removal\n          properties[index] = rollbackProperty\n      }\n      \n  } catch {\n      print("Failed with error: ", error)\n      // Undo the removal\n      properties[index] = rollbackProperty\n  }\n}\n'})}),"\n",(0,r.jsx)(t.h2,{id:"complete-example",children:"Complete example"}),"\n",(0,r.jsxs)(i,{children:[(0,r.jsx)(s,{name:"Main",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'import SwiftUI\nimport Amplify\nimport AWSAPIPlugin\n\n@main\nstruct OptimisticUIApp: App {\n    \n    init() {\n        do {\n            Amplify.Logging.logLevel = .verbose\n            try Amplify.add(plugin: AWSAPIPlugin(modelRegistration: AmplifyModels()))\n            try Amplify.configure()\n            print("Amplify configured with API, Storage, and Auth plugins!")\n        } catch {\n            print("Failed to initialize Amplify with \\(error)")\n        }\n    }\n    \n    var body: some Scene {\n        WindowGroup {\n            RealEstatePropertyContainerView()\n        }\n    }\n}\n\n// Extend the model to Identifiable to make it compatible with SwiftUI\'s `ForEach`.\nextension RealEstateProperty: Identifiable { }\n\nstruct TappedButtonStyle: ButtonStyle {\n    func makeBody(configuration: Configuration) -> some View {\n        configuration.label\n            .padding(10)\n            .background(configuration.isPressed ? Color.teal.opacity(0.8) : Color.teal)\n            .foregroundColor(.white)\n            .clipShape(RoundedRectangle(cornerRadius: 10))\n    }\n}\n'})})}),(0,r.jsx)(s,{name:"Actor",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'actor RealEstatePropertyList {\n    \n    private var properties: [RealEstateProperty?] = [] {\n        didSet {\n            subject.send(properties.compactMap { $0 })\n        }\n    }\n    \n    private let subject = PassthroughSubject<[RealEstateProperty], Never>()\n    var publisher: AnyPublisher<[RealEstateProperty], Never> {\n        subject.eraseToAnyPublisher()\n    }\n    \n    func listProperties() async throws {\n        let result = try await Amplify.API.query(request: .list(RealEstateProperty.self))\n        guard case .success(let propertyList) = result else {\n            print("Failed with error: ", result)\n            return\n        }\n        properties = propertyList.elements\n    }\n    \n    func createProperty(name: String, address: String? = nil) {\n        let property = RealEstateProperty(name: name, address: address)\n        // Optimistically send the newly created property, for the UI to render.\n        properties.append(property)\n        \n        Task {\n            do {\n                // Create the property record\n                let result = try await Amplify.API.mutate(request: .create(property))\n                guard case .failure(let graphQLResponse) = result else {\n                    return\n                }\n                print("Failed with error: ", graphQLResponse)\n                // Remove the newly created property\n                if let index = properties.firstIndex(where: { $0?.id == property.id }) {\n                    properties.remove(at: index)\n                }\n            } catch {\n                print("Failed with error: ", error)\n                // Remove the newly created property\n                if let index = properties.firstIndex(where: { $0?.id == property.id }) {\n                    properties.remove(at: index)\n                }\n            }\n        }\n    }\n\n    func updateProperty(_ property: RealEstateProperty) async {\n        guard let index = properties.firstIndex(where: { $0?.id == property.id }) else {\n            print("No property to update")\n            return\n        }\n\n        // Optimistically update the property, for the UI to render.\n        let rollbackProperty = properties[index]\n        properties[index] = property\n        \n        do {\n            // Update the property record\n            let result = try await Amplify.API.mutate(request: .update(property))\n            guard case .failure(let graphQLResponse) = result else {\n                return\n            }\n            print("Failed with error: ", graphQLResponse)\n            properties[index] = rollbackProperty\n        } catch {\n            print("Failed with error: ", error)\n            properties[index] = rollbackProperty\n        }\n    }\n        \n    func deleteProperty(_ property: RealEstateProperty) async {\n        guard let index = properties.firstIndex(where: { $0?.id == property.id }) else {\n            print("No property to remove")\n            return\n        }\n        \n        // Optimistically remove the property, for the UI to render.\n        let rollbackProperty = properties[index]\n        properties[index] = nil\n        \n        do {\n            // Delete the property record\n            let result = try await Amplify.API.mutate(request: .delete(property))\n            switch result {\n            case .success:\n                // Finalize the removal\n                properties.remove(at: index)\n            case .failure(let graphQLResponse):\n                print("Failed with error: ", graphQLResponse)\n                // Undo the removal\n                properties[index] = rollbackProperty\n            }\n            \n        } catch {\n            print("Failed with error: ", error)\n            // Undo the removal\n            properties[index] = rollbackProperty\n        }\n    }\n}\n\n'})})}),(0,r.jsx)(s,{name:"View",children:(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{className:"language-swift",children:'class RealEstatePropertyContainerViewModel: ObservableObject {\n    @Published var properties: [RealEstateProperty] = []\n    var sink: AnyCancellable?\n    \n    var propertyList = RealEstatePropertyList()\n    init() {\n        Task {\n            sink = await propertyList.publisher\n                .receive(on: DispatchQueue.main)\n                .sink { properties in\n                    print("Updating property list")\n                    self.properties = properties\n            }\n        }\n    }\n    \n    func loadList() {\n        Task {\n            try? await propertyList.listProperties()\n        }\n    }\n    func createPropertyButtonTapped(name: String) {\n        Task {\n            await propertyList.createProperty(name: name)\n        }\n    }\n    \n    func updatePropertyButtonTapped(_ property: RealEstateProperty) {\n        Task {\n            await propertyList.updateProperty(property)\n        }\n    }\n\n    func deletePropertyButtonTapped(_ property: RealEstateProperty) {\n        Task {\n            await propertyList.deleteProperty(property)\n        }\n    }\n}\n\nstruct RealEstatePropertyContainerView: View {\n    @StateObject var viewModel = RealEstatePropertyContainerViewModel()\n    @State private var propertyName: String = ""\n    \n    var body: some View {\n        VStack {\n            ScrollView {\n                LazyVStack(alignment: .leading) {\n                    ForEach($viewModel.properties) { $property in\n                        HStack {\n                            TextField("Update property name", text: $property.name)\n                                .textFieldStyle(RoundedBorderTextFieldStyle())\n                                .multilineTextAlignment(.center)\n                            Button("Update") {\n                                viewModel.updatePropertyButtonTapped(property)\n                            }\n                            Button {\n                                viewModel.deletePropertyButtonTapped(property)\n                            } label: {\n                                Image(systemName: "xmark")\n                                    .foregroundColor(.red)\n                            }\n                            \n                        }.padding(.horizontal)\n                    }\n                }\n            }.refreshable {\n                viewModel.loadList()\n            }\n            TextField("New property name", text: $propertyName)\n                .textFieldStyle(RoundedBorderTextFieldStyle())\n                .multilineTextAlignment(.center)\n            \n            Button("Save") {\n                viewModel.createPropertyButtonTapped(name: propertyName)\n                self.propertyName = ""\n            }\n            .buttonStyle(TappedButtonStyle())\n        }.task {\n            viewModel.loadList()\n        }\n    }\n}\n\nstruct RealEstatePropertyContainerView_Previews: PreviewProvider {\n    static var previews: some View {\n        RealEstatePropertyContainerView()\n    }\n}\n'})})})]})]})}let p=function(e={}){let{wrapper:t}=Object.assign({},(0,a.R)(),e.components);return t?(0,r.jsx)(t,Object.assign({},e,{children:(0,r.jsx)(l,e)})):l(e)};function d(e,t){throw Error("Expected "+(t?"component":"object")+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}var u=!0;let c={title:"オプティミスティックUI",description:"Amplify GraphQL APIでオプティミスティックUIを実装する方法について詳しく説明します。",platforms:["javascript","swift","angular","nextjs","react","vue"]};function y(e){let{Fragments:t}=Object.assign({},(0,a.R)(),e.components);return t||function(e,t){throw Error("Expected "+(t?"component":"object")+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}("Fragments",!0),(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(t,{fragments:{javascript:s,angular:s,nextjs:s,react:s,vue:s}}),"\n","\n",(0,r.jsx)(t,{fragments:{swift:p}})]})}let h=function(e={}){let{wrapper:t}=Object.assign({},(0,a.R)(),e.components);return t?(0,r.jsx)(t,Object.assign({},e,{children:(0,r.jsx)(y,e)})):y(e)}},91663:(e,t,n)=>{(window.__NEXT_P=window.__NEXT_P||[]).push(["/gen1/[platform]/build-a-backend/graphqlapi/optimistic-ui",function(){return n(83089)}])}},e=>{e.O(0,[9987,9434,90636,46593,38792],()=>e(e.s=91663)),_N_E=e.O()}]);