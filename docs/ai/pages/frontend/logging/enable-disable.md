---
title: "ロギングの有効化と無効化"
section: "frontend/logging"
platforms: ["android", "swift"]
gen: 2
last-updated: "2026-03-25T17:40:00.000Z"
url: "https://docs.amplify.aws/react/frontend/logging/enable-disable/"
---

このセクションでは、Amplify Loggerを使用する際にロギングを有効または無効にする方法を学習します。デフォルトでは有効になっていますが、`amplifyconfiguration_logging`ファイルで無効にすることができます。

<!-- Platform: android -->

#### [Java]

Amplify Loggerを有効にします。

```java
Amplify.Logging.enable();
```

Amplify Loggerを無効にします。

```java
Amplify.Logging.disable();
```

#### [Kotlin]

Amplify Loggerを有効にします。

```kotlin
Amplify.Logging.enable()
```

Amplify Loggerを無効にします。

```kotlin
Amplify.Logging.disable()
```

#### [RxJava]

Amplify Loggerを有効にします。

```java
Amplify.Logging.enable();
```

Amplify Loggerを無効にします。

```java
Amplify.Logging.disable();
```

<!-- /Platform -->

<!-- Platform: swift -->
Amplify Loggerを有効にします。

```swift
Amplify.Logging.enable()
```

Amplify Loggerを無効にします。

```swift
Amplify.Logging.disable()
```
<!-- /Platform -->
